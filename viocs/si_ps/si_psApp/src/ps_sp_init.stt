program ps_sp_init

int event_proc;
assign event_proc to "EVENT-SIPS-SPINIT.PROC";

ss ss1 {
    state init {
        when (delay(.5)) {
            event_proc = 1;
            pvPut(event_proc);
        } exit
    }
}
