program rf_sp_init

int event_proc;
assign event_proc to "EVENT-SIRF-SP-INIT.PROC";

ss ss1 {
    state init {
        when (delay(1.0)) {
            event_proc = 1;
            pvPut(event_proc);
        } exit
    }
}
